server:
  port: 3014
spring:
  datasource:
    url: jdbc:mysql://39.104.137.220:3306/logistics_db
    username: shixun
    password: shixun
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: login-register
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.182.6:8848
    #      config:
    #        server-addr: 192.168.182.6:8848
    #        file-extension: yaml
    #        prefix: application-login-register
    #        group: DEFAULT_GROUP
    #        namespace: #指定名称空间
    sentinel: #限流服务器sentinel配置
      transport:
        dashboard: localhost:1111
      #====================持久化配置=================================
      datasource:
        flow:
          nacos: #限流持久配置
            server-addr: 192.168.182.6:8848    #使用nacos的持久
            dataId: application-login_register    #获取限流的数据源的dataId
            namespace: #指定名称空间
            groupId: DEFAULT_GROUP
            rule-type: flow #类型：限流
      #=====================================================
  redis:
    database: 0
    host: 127.0.0.1
    port: 6379
    password: tianlong
    jedis:
      pool:
        max-wait: 1500ms
        min-idle: 2
        max-idle: 8
mybatis:
  mapper-locations: classpath:cn/neusoft/mapper/*Mapper.xml
